{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["iad1"],
  "buildCommand": "prisma generate && prisma migrate deploy && tsx scripts/ensure-demo-admins.ts && tsx scripts/ensure-company-logos.ts && next build"
}
