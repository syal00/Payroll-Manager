DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@DIRECT-HOST.neon.tech/neondb?sslmode=require"
AUTH_SECRET="run: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""
# Base domain for subdomain-based tenant resolution in proxy.ts, e.g. "payrollapp.com" in
# production (so "acme.payrollapp.com" resolves the "acme" tenant). Defaults to "localhost" for dev.
ROOT_DOMAIN="localhost"
