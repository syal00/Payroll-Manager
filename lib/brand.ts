/** Product name shown in UI when no tenant-specific branding is set. */
export const APP_NAME = "PayRun";

export const DEFAULT_BRAND_NAME = APP_NAME;

export const SUPPORT_EMAIL = "hello@payrun.app";
